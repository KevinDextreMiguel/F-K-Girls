export interface ProductVariant {
  color: string;         // nombre del color (opcional)
  hex: string;           // código del color para el punto
  image: string;         // imagen de esa variante
}